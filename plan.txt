I want to modify my existing LearningHUB application to automatically seed the initial admin account via environment variables on startup, disable public registration, and prepare the entire stack for free-tier deployment on Vercel (Frontend), Render (Backend), and Supabase (Database).

Please write the exact code and deployment configurations for the following requirements:


1. In 'learninghub-backend/app/__init__.py':
   Create a startup initialization function inside the app factory context that checks if a user with the role 'Admin' exists in the MySQL database. If not, automatically create and save this user using ADMIN_EMAIL and ADMIN_PASSWORD from the .env file. The password must be safely hashed using bcrypt.

2. In 'learninghub-backend/app/routes/auth.py':
   Completely remove or disable the public `POST /register` endpoint so that unauthorized users cannot create accounts from the web.

3. In 'learninghub-frontend/':
   Explain how to clean up 'src/App.jsx' and delete 'src/pages/Register.jsx' since registration is no longer needed.
4.  Email OTP Verification System using Python's built-in SMTP libraries for creating account .
My current tech stack is:
- Backend Framework: Flask 3.1.1 & Flask-SQLAlchemy
- Encryption/Auth: bcrypt & Flask-JWT-Extended
- Database: MySQL
- Frontend Framework: React 18 + Vite + Tailwind CSS

Please show me the exact code snippets and structural placements for each of these files.


1. BACKEND CHANGES & ADMIN SEEDING:
   - In 'learninghub-backend/app/__init__.py': Write an initialization function within the app factory context. If a user with the role 'Admin' does not exist in the database, automatically create it using ADMIN_EMAIL and ADMIN_PASSWORD from the environment variables (passwords must be hashed using bcrypt).
   - In 'learninghub-backend/app/routes/auth.py': Remove or disable the public `POST /register` endpoint.
   - Database Adapter: My project README lists PyMySQL, but since Supabase provides a PostgreSQL database, please tell me which dependency to add to requirements.txt (e.g., psycopg2-binary) and how to format the connection string.

2. RENDER CONFIGURATION (Flask Backend):
   - Provide a 'render.yaml' file or blueprint to host this Flask app on Render's free tier. Include instructions on how to set up Gunicorn as the WSGI server using 'run.py'.

3. VERCEL CONFIGURATION (React Frontend):
   - Provide a 'vercel.json' configuration file for the React + Vite frontend to prevent routing errors (404 on refresh) by rewriting all requests to index.html.

4. ENVIRONMENT VARIABLE GUIDE:
   - Provide a clean list of the exact Environment Variables I need to copy and paste into the Supabase, Render, and Vercel dashboards.

My production tech stack is:
- Frontend: React 18 + Vite + Tailwind CSS (Deploying to Vercel Free Tier)
- Backend: Flask 3.1.1 + Flask-SQLAlchemy + Flask-JWT-Extended (Deploying to Render Free Tier)
- Database: Supabase PostgreSQL (Replacing local MySQL for cloud deployment)
